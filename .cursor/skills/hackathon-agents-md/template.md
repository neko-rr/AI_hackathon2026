# AGENTS.md テンプレート

プロジェクトルートに `AGENTS.md` として出力する。`{...}` はプロジェクト情報で置換。不要セクションは削除。

---

```markdown
# AGENTS.md

> ハッカソン開発用の AI エージェント指示書。チーム全員で共有・更新する。

## プロジェクト概要

- **大会**: {大会名}
- **チーム**: {人数}名（{メンバー1}: {役割} / {メンバー2}: {役割}）
- **開発時間**: {例: 3時間}
- **目的**: {一言で何を作るか}
- **詳細**: [docs/{event-slug}-summary.md](docs/{event-slug}-summary.md)

## ハッカソン制約

レギュレーション詳細 → [docs/{event-slug}-regulation-summary.md](docs/{event-slug}-regulation-summary.md)

- 開発は**開始合図後のみ**（事前のコード実装禁止。企画・調査は可）
- 開発中の **AI 利用必須**（成果物への AI 組み込みは不要）
- プレゼン **{例: 5}分** / デモ必須 / Q&A {あり|なし}
- 提出期限: **{例: 14:00}**（Google Drive + QR）
- 持ち込み: ノートPC各自1台

## チーム運用

- **意思決定**: {例: テーマ告知後15分以内に MVP を確定}
- **実装**: {担当者} が主導
- **発表・資料**: {担当者} が主導
- **共有**: `docs/idea.md` にスコープ、`docs/tasks.md` に進捗
- **Git**: `{main|develop}` に直接 push 可 / PR 必須 → {選ぶ}

## ディレクトリ構成

| パス | 用途 |
|------|------|
| `src/` | アプリケーション本体 |
| `docs/` | 要約・idea・tasks・demo-script |
| `assets/` | スライド・デモ用画像 |
| `notes/` | 調査メモ（参照用、コード import 禁止） |
| `.env` | 秘匿情報（**Git 管理外**） |

## 技術スタック

| 項目 | 採用 | 備考 |
|------|------|------|
| 言語 | {例: TypeScript} | |
| フレームワーク | {例: Next.js 15} | |
| AI/LLM | {例: OpenAI API} | 開発中に必ず使用 |
| デプロイ | {例: Vercel} | デモ URL 用 |

## コマンド

```bash
# 初回セットアップ
{例: cp .env.example .env && npm install}

# 開発サーバー
{例: npm run dev}

# ビルド（デプロイ前確認）
{例: npm run build}

# リント（時間があれば）
{例: npm run lint}
```

## 開発方針

- **MVP 優先**: デモで見せる1フローに絞る
- **縦スライス**: UI → API → AI の最小経路を先に通す
- **新規導入**: チームが既知の技術を優先。初見の FW は避ける
- **エラー処理**: デモパスのみ。エッジケースは後回し
- **UI**: 見た目より操作の確実性。デモで失敗しない経路を確保

## コーディング規約

- {例: TypeScript strict / ESLint 設定に従う}
- 関数・変数名: 英語、意味が明確な名前
- コメント: 非自明なロジックのみ（日本語可）
- 1ファイル1責務。200行超えたら分割を検討

## AI エージェント向け指示

### やること

- 変更前に `docs/idea.md` の MVP スコープを確認
- 小さな差分で提案。1タスク = 1機能
- デモ経路が動くことを最優先
- 外部 API 呼び出しは try-catch とタイムアウト

### やらないこと

- API キー・`.env` の値をコードやログに出力
- 大規模リファクタ・ディレクトリ再編（提案のみ）
- デモに不要な機能追加（「ついでに」禁止）
- `node_modules/` 等の自動生成物を手編集
- レギュレーション違反（事前開発、テンプレ流用等）

### 判断に迷ったら

1. デモで見せられるか？
2. 3時間（または残り時間）で終わるか？
3. チームの既知スタックか？

## ドキュメント

| ファイル | 更新タイミング |
|---------|---------------|
| [docs/idea.md](docs/idea.md) | テーマ確定後 |
| [docs/tasks.md](docs/tasks.md) | タスク・進捗変更時 |
| [docs/demo-script.md](docs/demo-script.md) | デモ確定後 |

## 変更履歴

| 日付 | 内容 |
|------|------|
| {YYYY-MM-DD} | 初版作成 |
```

---

## プレースホルダ一覧

| プレースホルダ | 取得元 |
|---------------|--------|
| `{大会名}` | docs/*-summary.md |
| `{event-slug}` | 要約ファイル名 |
| `{開発時間}` | レギュレーション要約 |
| `{提出期限}` | docs/tasks.md |
| 技術スタック | idea.md またはユーザー確認 |
